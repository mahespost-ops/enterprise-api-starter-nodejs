/**
 * Network Constants
 * IP addresses and network-related defaults
 */

export const NETWORK_DEFAULTS = {
  LOCALHOST_IP: '127.0.0.1',
} as const;

export const DEFAULT_CONTEXT = {
  ORG_ID: 'default-org-id',
  ENV_ID: 'default-env-id',
} as const;
